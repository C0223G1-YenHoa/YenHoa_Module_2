   for (String s : array) {
            String a=s.toUpperCase();
            if (map.containsKey(a)) {
                map.put(a,map.get(a)+1);
            } else {
                map.put(a, 1);
            }
        }